# 策略规格说明（代码逻辑蒸馏）

本文是对当前实现的行为规格化描述，而非投资建议。默认参数以
`config/strategy.yaml` 为例；每个股票可在 `basket` 中覆盖部分参数。

## 1. 策略定位

- 标的：仅股票、仅做多（LONG-ONLY）。
- 风格：价格动量触发，叠加瞬时成交量放大与成交额流动性过滤。
- 执行：买入使用带滑点上限的限价单；平仓使用市价单；持仓期间维持
  IBKR 托管的灾难性 STOP MKT 保护单。
- 运行：单一主循环轮询行情与券商状态。策略启动后默认 `PAUSED`，须人工
  `start` 才允许新增仓位。
- 非目标：当前实现**没有**催化剂（新闻、财报、事件）输入，也没有“人工
  买入信号”；人工指令只能改变引擎状态或发起全平。

## 2. 标的池（Universe）

### 2.1 如何进入监控池

标的由 YAML 的 `basket` 显式列出。每一项至少定义 `symbol`，并带有交易所、
币种、市场、单票资金上限等合约与风险属性。启动时，系统会：

1. 校验配置、准备券商合约并取得参考价；参考价不可得时保留监控，但不会产生
   价格动量信号。
2. 为每个标的建立独立的行情监控器和仓位状态机。
3. 从 broker 读取仓位、核对本地持久化状态与订单意图账本；持仓以 broker 为准。

重复 symbol 或重复 `con_id` 会被拒绝。实盘港股还必须固定 `con_id`、
`local_symbol` 与 `trading_class`，避免合约歧义。

### 2.2 美股与港股的隔离

| 维度 | 美股（US） | 港股（HK） |
| --- | --- | --- |
| 市场/币种 | US / USD | HK / HKD |
| 允许交易时段 | 配置的 pre、regular、post（默认全开） | 仅 HKEX 连续交易时段；午休不可买入 |
| 成交额门槛 | 当前 pre/regular/post 时段分别累计 | 当地交易日累计，午休不清零 |
| 流动性默认门槛 | `us_min_session_turnover_usd`，默认 1,000,000 USD | `hk_min_daily_turnover_hkd`，默认 1,000,000 HKD |
| 数量约束 | 通常整股 | 必须按已配置并核验的 board lot 向下取整 |
| 额外买入约束 | 可配置盘前盘后 | 强制 regular、`outsideRth=false`、正费率准备金、已结算 HKD 现金校验 |

一个 live 进程只能运行一个市场和一个报价币种；USD 与 HKD 标的不能混在同一
资金、PnL 和熔断账本中，必须分进程运行。

## 3. 输入与数据有效性

| 输入 | 用途 | 失效时的行为 |
| --- | --- | --- |
| 最新成交价 | 动量、持仓峰值、移动止损、浮盈 | 无效/非正/不新鲜时不产生买入，也不以该价格触发本地软止损 |
| 累计成交量 | 计算每秒增量与滚动成交量基线 | 无效时不形成有效量能信号 |
| 累计成交额与时间戳 | 通过买入流动性闸门 | 非实时、超过 `turnover_max_age_sec`、跨市场时段、回退或异常时 fail closed：禁止买入/加仓 |
| 参考价 | 当前价相对约 `lookback_hours` 前的涨幅 | 不可得则动量信号为 false |
| 市场日历/时段 | 判断交易所开放和可下买单时段 | 非交易日、收市和港股午休禁止买入/加仓；退出逻辑不受此闸门限制 |
| broker 仓位、订单账本、连接状态 | 对账、数量真值和下单授权 | 未知、订单未确认、断线重连后未完成对账、持仓漂移时禁止新单并 HALTED |
| 已结算报价币种现金 | 港股买入资金约束（可泛化开启） | 缺失、无效或不足时禁止买入 |
| 人工命令 | `start`、`pause`、`close_all`、`reset` | 不构成个股入场信号 |

成交量基线为最近 300 秒内有观测的每秒成交量增量均值；默认至少积累 120 秒，
且均值不低于 `volume_min_avg`。注意“当前秒成交量”必须**严格大于**倍数阈值，
不是大于等于。

## 4. 入场与加仓

### 4.1 首次入场条件

同一标的必须同时满足以下条件：

1. 引擎为 `RUNNING`，持久化状态健康，券商连接/账户可下单，且不存在
   `order_uncertain`。
2. 标的处于允许的交易所交易时段。
3. 成交额流动性闸门已通过：累计成交额**严格大于**对应阈值，且数据实时、
   新鲜并属于当前时段/交易日。
4. 价格动量：`最新价 / 参考价 - 1 >= price_gain_threshold_pct`（默认 +8%）。
5. 量能突发：达到预热要求、平均每秒成交量满足下限，且当前秒成交量
   `> volume_surge_ratio × 基线`（默认 > 3 倍）。
6. 标的本地状态为 `IDLE`，没有正在处理的订单；可用策略资金（及需要时的
   已结算报价币种现金）足够买入至少一手。

满足条件后提交 BUY 限价单，限价上沿为 `市价 × (1 + slippage_pct)`；默认允许
1% 滑点。部分成交会以 broker 实际仓位/均价为准接管。成功持仓后必须先确认
灾难性 STOP 已被托管；保护失败会在下一轮使引擎停摆。

### 4.2 加仓条件与优先级

对已有仓位，每轮先执行退出判断，再考虑加仓。加仓必须同时满足：

1. 退出条件均未触发；
2. 所有 `RUNNING`、时段、成交额、状态健康和可用现金约束仍成立；
3. 最新价 `>= avg_cost × (1 + scale_in_gain_pct)`；默认高于持仓均价 10%；
4. 当前数量低于单票最大可持数量；完整的下一基础批次可负担。

加仓批次与首次基础批次相同，但不会为了凑现金而缩小为零碎小批次；若不足以
承担完整批次，则跳过。本轮按 basket 顺序逐标的重算可用资金，前一笔成交会影响
后一标的可用额度。

## 5. 仓位与数量计算

令：

- `C` = `strategy.capital`；若为 0，则为 basket 各标的资金上限之和；
- `A_i` = 标的 i 的配置资金上限；
- `P` = 当前市价；
- `s` = `slippage_pct`；`f` = `estimated_fee_pct`；
- `L` = lot size（美股通常为 1，港股为 board lot）。

单股保守成本为 `u = P × (1+s) × (1+f)`。对标的 i：

- 基础名义额度：`B_i = min(base_position_pct × C, A_i)`；
- 单票最大名义额度：`M_i = min(max_position_pct × C, A_i)`；
- 初始数量：`floor_to_lot(B_i / u, L)`；
- 最大数量：`floor_to_lot(M_i / u, L)`。

最终订单数量还需不超过策略可用资金：
`C - Σ(现有仓位成本) + Σ(已实现 PnL)`，以及开启现金校验时的 broker 已结算
报价币种现金。若最小一手不可负担，则不下单。

因此，单票上限同时受 `max_position_pct` 和 `max_allocation[_usd]` 约束；全策略
成本以总资本为界，且每次下单前重新计算，避免同一轮多笔买入合计超额。估算费用
既计入下单资金检查，也从内部 PnL 扣除，因而只会让熔断更早而非更晚。

## 6. 退出与停摆

所有现有仓位的退出判断先于加仓，且即使引擎 `PAUSED` 仍持续执行。

| 类型 | 触发条件 | 动作 |
| --- | --- | --- |
| 浮盈回撤保护 | 浮盈曾为正，且当前浮盈 `<= peak_unrealized × (1-profit_drawdown_stop_pct)` | 撤/核对保护单后市价全平 |
| 移动止损 | `价格 <= peak_price × (1-stop_loss_pct)` | 市价全平 |
| 灾难止损 | broker 托管 STOP MKT；触发价为 `avg_cost × (1-catastrophe_stop_pct)` | 在本地进程断线/崩溃时由 broker 执行 |
| 全局熔断 | 策略管理仓位的已实现 + 未实现 PnL 损失达到 `aum_loss_limit_pct × C`（默认 5%） | 全部平仓，引擎 `HALTED` |
| 人工退出 | `close_all` | 全部平仓，随后引擎 `PAUSED` |
| 人工暂停 | `pause` | 禁止入场/加仓；不关闭已有止损 |

灾难止损比例必须**严格大于**本地移动止损比例，以便正常情况下本地移动止损先执行；
它是掉线保护而非独立交易信号。平仓时以 broker 仓位为唯一数量真值：先确认撤销
保护 SELL，再读取 broker 剩余数量并发送相应的市价 SELL，最后轮询确认归零。若
撤单状态不确定，拒绝发送可能造成空头的第二笔 SELL，转入人工处置。

止损/全平后，`allow_reentry_after_stop=false`（默认）时标的进入 `HALTED`，同一
会话不可自动再进；为 true 时回到 `IDLE` 可再次等待信号。`reset` 只允许在引擎
非 RUNNING 且 broker/local 均确认空仓时清除 HALTED，之后仍须再次 `start`。

“catalyst invalidation”在当前代码中未实现，因此不存在由催化剂失效触发的平仓。

## 7. 状态机

实现有两个层级状态：引擎状态控制是否能发起 BUY；每标的仓位状态控制其持仓生命周期。

```text
引擎

PAUSED -- start（所有启动闸门通过） --> RUNNING
RUNNING -- pause / close_all ----------------> PAUSED
RUNNING/PAUSED -- 风控、对账、保护失败 -----> HALTED
HALTED -- reset（全部确认空仓） ------------> PAUSED

标的

IDLE -- 入场条件 + BUY 成交 ----------------> ENTERED
ENTERED -- 加仓条件 + BUY 成交 -------------> ENTERED
ENTERED -- 止损 / 熔断 / 手动全平 ----------> HALTED（默认）或 IDLE（允许再入场）
HALTED -- reset（确认空仓） ----------------> IDLE
```

用户示例中的 `WATCHING` 对应“引擎任意状态下持续轮询行情”；`TRIGGERED` 和
`ORDER_PENDING` 在实现中不是持久化枚举，而是主循环中检测到信号、以及
`_in_flight=true` 的短暂过程。`POSITIONED` 对应标的 `ENTERED`；`EXITING` 对应
撤灾难 STOP、读取 broker 数量、发市价平仓并确认归零的同步流程。

## 8. 安全不变量

1. `PAUSED`、`HALTED`、持久化失败、券商未就绪或订单状态不确定时，绝不允许
   新入场或加仓。
2. 每标的仅在 `IDLE` 时首次入场；订单处理中 `_in_flight` 阻止重入。同一信号
   不会在持仓状态下重复生成 entry order。
3. 无有效价格、参考价、实时新鲜的成交额或合法交易时段时，BUY fail closed。
4. broker 仓位是数量与均价的真值；启动、重连与每个 live tick 都核对。发现
   broker/local 数量漂移即 HALTED，禁止继续交易。
5. 检测到 broker 空头仓位绝不自动再 SELL；立即 HALTED 并要求人工买回补空。
6. 每次 broker 提交前写订单意图账本；未能确认终态的订单不自动重试。
7. 已持仓必须有数量精确的 broker 托管灾难 STOP；无法建立、替换或确认该保护时
   引擎 HALTED。
8. 平仓先撤并确认保护 SELL，再按 broker 实际剩余数量出售；撤单不确定时宁可
   不重复 SELL，避免意外做空。
9. 单票持仓不超过 `min(max_position_pct × C, A_i)`，总策略成本不超过可用策略资金；
   港股还不得超过已结算 HKD 现金。
10. 配置禁止混合市场/币种；港股要求明确账户、合约约束、board lot、regular-only
    及专属账户标的确认。持久化身份与 HK broker 状态不匹配时启动失败。
11. 全局熔断只基于本策略管理仓位的已实现/未实现 PnL，不读取账户净值；触发后
    全平并需人工 `reset + start`。
12. `catastrophe_stop_pct > stop_loss_pct` 为启动时强制校验，避免 broker 保护单
    抢先破坏本地移动止损逻辑。

## 9. 当前默认参数速览

| 参数 | 默认值 | 含义 |
| --- | ---: | --- |
| `price_gain_threshold_pct` | 8% | 相对参考价的最低涨幅 |
| `volume_surge_ratio` | 3.0 | 当前秒成交量严格超过基线的倍数 |
| `lookback_hours` | 24 | 参考价回看窗口 |
| `volume_warmup_seconds` | 120 秒 | 量能信号预热期 |
| `base_position_pct` | 25% | 首次及每次加仓的目标基础额度 |
| `scale_in_gain_pct` | 10% | 相对均价的下一次加仓触发涨幅 |
| `max_position_pct` | 65% | 单票最大资金比例 |
| `stop_loss_pct` | 10% | 自峰值的移动止损回撤 |
| `profit_drawdown_stop_pct` | 60%（US 示例） | 从峰值浮盈的允许回撤比例 |
| `catastrophe_stop_pct` | 15% | 相对均价的 broker 托管灾难止损 |
| `aum_loss_limit_pct` | 5% | 策略级总损失熔断线 |

配置文件是最终参数真值；港股模板对浮盈回撤保护、手续费准备金、资金与交易时段
采用更保守的独立设置。
